#include "holberton.h"
/**
 * _strcat - concat strings
 * @dest: input 1
 * @src: input 2
 * Return: always 0
 */

char _strcat(char *dest, char *src)
{
int j, k;
i = 0;
j = strlen(dest)
k = strlen(src)
for (i = 0, i < j; i++)
{_putchar(dest[i]); }
for (i = 0, i <= k; i++)
{ _putchar(src[0]); }
return (0);
}
