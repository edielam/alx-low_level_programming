#include <stdio.h>
/**
 * main - print out hex
 * Return: 0
 */

int main(void)
{
int a;
int b;

for (a = 48; a <= 57; a++)
{putchar(a); }
for (b = 65; b <= 70; b++)
{putchar(b); }
putchar(10);
return (0);
}
